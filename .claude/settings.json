{
  "$comment": "Local development settings - hooks are now defined in plugin.json only to prevent duplication",
  "$note": "When working in this repo, CC loads both this file AND plugin.json. Hooks defined in plugin.json only."
}